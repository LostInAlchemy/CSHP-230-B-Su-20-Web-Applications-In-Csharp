@model IEnumerable<TheLearningCenter.WebSite.Models.ClassModel>

@{
    ViewBag.Title = "AddToCart";
}

<h2>AddToCart</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(ClassModel => ClassModel.ClassId)
        </th>
        <th>
            @Html.DisplayNameFor(ClassModel => ClassModel.ClassDescription)
        </th>
        <th>
            @Html.DisplayNameFor(ClassModel => ClassModel.ClassPrice)
        </th>
        <th></th>
    </tr>

    @foreach (var item in EnrolledClasses)
    {
        <tr>
            <td>
                @Html.DisplayFor(EnrolledClasses => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>